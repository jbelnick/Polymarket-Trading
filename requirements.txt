# Polymarket Trading Bot — Dependencies
#
# Install: pip install -r requirements.txt
# Or with uv: uv pip install -r requirements.txt

# Data analysis (Step 0: wallet profiling)
polars>=1.0.0

# Claude API (Step 2: the brain)
anthropic>=0.40.0

# Polymarket CLOB client (Step 3: order execution)
py-clob-client>=0.1.0

# Async HTTP (market data fetching)
httpx>=0.27.0
